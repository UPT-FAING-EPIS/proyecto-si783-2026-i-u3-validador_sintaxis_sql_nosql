# Descargas del frontend

Esta carpeta es servida por Express porque vive dentro de `frontend`.

Coloca aqui los paquetes publicados para que los enlaces de la seccion Integraciones funcionen:

- `sqlcheck-linux.tar.gz`
- `sqlcheck_1.0.0_amd64.deb`
- `sqlcheck-1.0.0.x86_64.rpm`

Los artefactos fuente se generan primero en `releases/cli`.
